#ifndef INCLUDE_GUARD_RADIUS
#define INCLUDE_GUARD_RADIUS
#include "../headers/radius.h"
#endif

Radius::Radius() {
    this->radius = 0;
}

Radius::Radius(float radius) {
    this->radius = radius;
}
